; ~/.config/gammastep/config.ini — night light
; Started from i3 config (only if gammastep is installed).
[general]
temp-day=6500
temp-night=4200
fade=1
adjustment-method=randr
location-provider=manual

[manual]
; Zagreb — adjust to your actual location if needed
lat=45.81
lon=15.98
